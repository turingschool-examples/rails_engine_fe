<%= "Merchant #{@merchant.id}"%>

<section id="items">
  <% @items.each do |item| %>
    <p>
      Name: <%= link_to "#{item.name}", item_path(item.id) %><br>
      Description: <%= item.description %><br>
      Unit Price: <%= item.unit_price %><br>
    </p>
  <% end %>
</section>